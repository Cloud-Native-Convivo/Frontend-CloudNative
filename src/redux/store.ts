/**
 * Placeholder para configuración futura de Redux Toolkit.
 * Actualmente el estado global reside en src/context/AuthContext.tsx.
 */

export interface RootState {
  // Reserved for future slices (e.g. auth, notification, espacios)
  auth?: unknown;
}

export const storePlaceholder = {
  name: "convivo-store-placeholder",
  version: "1.0.0",
};
